# /examples

This folder will contain anonymised sample `.fp` fingerprint files
for testing and demonstration purposes.

## Coming Soon

- `sample_researcher.fp` — anonymised from a research-focused conversation
- `sample_creative.fp` — anonymised from a creative writing session

## Generate Your Own

```bash
python src/encode.py --input my_conversation.json --output examples/my_identity.fp
```

See `/src/encode.py` for input format details.
